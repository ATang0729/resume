# 履历母库与投递版创建说明

## 1. 文件定位

- `index.html`：中文完整履历母库，不限页数，原则上只增加、修正或标记过期内容，不因某次投递删除经历。
- `index-en.html`：英文完整履历母库，与中文版保持经历、时间和数据口径一致。
- 投递版 HTML：未来按需新建，例如 `ai-product.html`、`technical-operations.html`、`agent-engineer.html`。投递版可精简，但不反向覆盖母库。
- `style.css`：网页端响应式视觉与 A4 打印样式；定制版应复用该设计变量与打印原则。
- `resume.js`：深色模式等渐进增强；核心履历内容不得依赖 JavaScript 才能显示。

## 2. 不可违反的维护原则

1. 先补充母库，再生成投递版。
2. 未经用户确认，不得从母库删除经历、项目、奖项或技术细节。
3. 信息不完时使用“待补充”标记，不自行虚构数据，也不删除该条记录。
4. 量化结果保留原始口径；如需改写，不改变数值、样本量、时间或因果关系。
5. 同一经历可在不同投递版采用不同表达，但事实应与母库一致。
6. 每次定制前应先保留或提交当前版本，避免覆盖未保存内容。
7. 用户最新人工修改并确认的简历与明确事实纠正，优先级高于旧投递版、生成脚本、QA 文本和历史改写；旧投递版不得反向充当事实证据。
8. 每段实习或工作经历必须保留经来源验证的工作地点；来源不足时先标记并询问，不得根据公司名称自行推断。

## 3. 可接受的用户输入

用户不需要先整理成标准简历，可以直接提供以下任意内容：

- 一段自由文字或复盘记录。
- 公司、岗位、时间加几条工作事项。
- 旧版 HTML、Markdown、Word、PDF 或纯文本简历。
- 项目周报、汇报材料、PRD、产品截图或项目总结。
- 目标岗位 JD，以及希望强调或弱化的能力。
- 既有简历的整段粘贴内容，允许格式混乱或存在 HTML 实体编码。

收到非结构化内容后，应先提取事实、标记冲突与缺失项，再写入母库。

## 4. 建议的经历补充模板

如果方便，可按下列模板提供；不强制填满所有字段。

```text
公司/项目：
岗位/角色：
城市：
时间：
背景与目标：
用户/业务痛点：
我负责的工作：
关键方案或技术：
协作对象：
交付物：
量化成果：
使用工具/技术：
可证明的链接/材料：
希望强调的方向：AI产品 / 技术运营 / Agent工程 / 其他
```

## 5. 从母库创建投递版的流程

1. 读取目标 JD，提取硬性条件、核心任务、能力词与业务场景。
2. 从母库按“相关性、证据强度、时间新近度”给经历和要点评分。
3. 选择 3–5 段核心经历与 2–4 个项目，每段保留 1–3 条最强证据。
4. 将表达改写为“问题/目标—关键动作—量化结果—能力映射”，但不改变事实。
5. 生成新的独立 HTML，不直接编辑 `index.html` 或 `index-en.html`。
6. 进行 HTML / DOCX 校验、A4 打印渲染、页数、断页、文字可提取性和 ATS 关键词检查。
7. 输出一份“选择了什么/暂未选择什么”的摘要，未入选内容仍留在母库。

### 投递文件命名与版式

- 最终 DOCX 与 PDF 统一命名为 `姓名-应聘岗位-学校-专业.ext`，字段之间使用半角连字符；除非用户明确要求，不额外加入公司名、版本号或“校招简历”等字样。
- 默认仅使用一页 A4，并在不牺牲可读性的前提下尽可能铺满页面；只有用户明确要求，或必要证据无法在一页内合理容纳并经用户确认时，才使用第二页。
- 压缩顺序为：删除低相关内容、去除重复、合并同一事件的要点、精简表达、微调模板内间距；不得通过虚构信息、隐藏文字或过度缩小字号实现单页。
- 当前默认字体为中文 Songti SC、英文 Times New Roman，正文 10 pt、章节标题 11 pt、1.15 倍行距；用户最新模板或明确要求优先。

## 6. 常见岗位的内容优先级

### AI 产品经理

优先顺序：合合信息 → 冰兰智能 → 上海容诚会计师事务所 → 小鹏汇天。

强调：场景抽象、PRD 与原型、Agent 产品设计、指标体系、跨部门落地、业务成果。

若 JD 同时强调 AI 行业跟踪、用户反馈或内容运营，可在核心 AI 产品经历后补充小鹏汇天与机智流：小鹏汇天提取用户访谈、需求转化和 AI 产品评测；机智流提取论文 / 开源生态跟踪、渠道运营、内容创作、用户反馈驱动的 Prompt 实验及 AI Skill / Agent 沉淀。投递版必须将公司实习放入“实习经历”，社区、创业和独立产品放入“项目经历”或对应的非工作经历章节，不得混排。

若 JD 明确偏 AI 语音交互 / 智能客服，优先顺序调整为：冰兰智能 → 合合信息 → 上海容诚会计师事务所 → 小鹏汇天。重点提取 ASR—LLM—TTS 链路、通话 Case 抽检与问题归因、回复自然度优化、替换词配置与模型输出后处理；不得在缺少数据时补写量化结果。

### 技术运营

技术运营需先判断 JD 更接近“开源 / 开发者社区”还是“ToB 客户 / AI 解决方案”，再从母库取材。

- **开源 / 开发者社区：**OpenMMLab MMPose → 书生·浦语实战营 → 机智流 → 技术写作 → 合合信息。强调 GitHub PR / Review / CI、开发者答疑、社群运营、开源生态追踪、技术内容、活动策划与流程自动化。
- **ToB 客户 / AI 解决方案：**合合信息 → 上海容诚会计师事务所 → 冰兰智能 → 小鹏汇天 → Kantar。强调需求澄清、技术概念转译、Agent / Workflow 选型、PoC 与评测、Agent Harness、反馈迭代、跨部门协同与结果交付。

通用强调：GEO、工具自动化、内容生产、数据运营、用户反馈闭环、渠道增长与效率提升。

### Agent 工程师

优先顺序：合合信息 → 上海容诚会计师事务所 → 冰兰智能 → LLM 业务规则项目。

强调：状态机、断点续跑、失败恢复、Skill 编排、ReAct、n8n、SFT/GRPO、Benchmark、Prompt 评测和 Python。

### 公司命名规范

中文统一使用“上海容诚会计师事务所”，英文统一使用“Shanghai Rongcheng CPA”；后续母库与投递版均不再添加旧品牌前缀。

## 7. 可直接使用的文字指令

### 补充母库

```text
请先阅读 resume/CUSTOMIZATION_GUIDE.md。以下是我新的经历/旧简历内容，请提取事实并补充到中英文履历母库。不要删除或压缩已有经历；如有数据冲突或信息不足，先标记并询问我。

[在此粘贴任意格式的经历、简历或复盘]
```

### 创建定制投递版

```text
请先阅读 resume/CUSTOMIZATION_GUIDE.md，再基于 resume/index.html 履历母库，为下方 JD 生成一份独立的一页中文投递版。不要修改母库。请说明经历取舍与关键词映射，并验证 A4 打印版式；最终文件按“姓名-应聘岗位-学校-专业”命名。

[在此粘贴 JD]
```
